---
name: saicmotor-suite
version: 1.0.0
description: "saicmotor 统一入口 skill——AI Agent 通过此 skill 发现并路由到具体业务能力"
metadata:
  requires:
    bins: ["saicmotor"]
---

> 此文件由 saicmotor 注册器在 skills 注册/刷新时自动生成，请勿手动编辑。
> 手动编辑的内容将在下次 `saicmotor install` 或 `saicmotor plugin install` 时被覆盖。